spring:
  application:
    name: activity-service
  data:
    mongodb:
      uri: mongodb+srv://samarjitdalai2001_db_user:6uoEe14tizmNNnJN@cluster0.dfurcql.mongodb.net/?appName=Cluster0
      database: fitnessactivity
  rabbitmq:
    host: localhost
    port: 5672
    username: ${RABBITMQ_USERNAME}
password: ${RABBITMQ_PASSWORD}

server:
  port: 8082

eureka:
  client:
    serviceUrl:
      defaultZone: http://localhost:8761/eureka/

rabbitmq:
  exchange:
    name: fitness.exchange
  queue:
    name: activity.queue
  routing:
    key: activity.tracking